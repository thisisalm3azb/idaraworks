# IdaraWorks environment variables (S0 checklist §10).
# Copy to .env.local for development. NEVER commit real values.
# Each var: owner = who provisions it; phase = when it becomes required.

# ── App (Phase A) ────────────────────────────────────────────────────────────
APP_ENV=dev                      # dev | preview | prod
# LOG_LEVEL=debug

# ── Supabase (Phase B) ───────────────────────────────────────────────────────
# NEXT_PUBLIC_SUPABASE_URL=       # owner: Supabase dashboard
# NEXT_PUBLIC_SUPABASE_ANON_KEY=  # auth only — data goes through the tenancy layer
# DATABASE_URL=                   # pooled (Supavisor transaction mode), role app_user
# DIRECT_URL=                     # direct connection — migrations only
# SUPABASE_SERVICE_ROLE_KEY=      # CI/migrations environment ONLY — never app runtime (phase2/10 #1)

# ── Queue / events (Phase G) ─────────────────────────────────────────────────
# INNGEST_EVENT_KEY=
# INNGEST_SIGNING_KEY=

# ── Email / SMS (Phase C) ────────────────────────────────────────────────────
# RESEND_API_KEY=
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_VERIFY_SID=

# ── Rate limiting (Phase C) ──────────────────────────────────────────────────
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# ── Observability (Phase I) ──────────────────────────────────────────────────
# SENTRY_DSN=
# SENTRY_AUTH_TOKEN=              # CI only (sourcemaps)

# ── Crypto (Phase B+) ────────────────────────────────────────────────────────
# FIELD_ENCRYPTION_KEY=           # 32 bytes, base64 — future integration credentials
